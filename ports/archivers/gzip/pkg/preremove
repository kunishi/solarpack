INFODIR=$CLIENT_BASEDIR/info
INSTALL_INFO=$CLIENT_BASEDIR/bin/install-info

if test -x ${INSTALL_INFO}; then
	${INSTALL_INFO} --delete --info-dir=${INFODIR} ${INFODIR}/gzip.info
else
	/usr/bin/echo ">> Remove gzip.info entry from ${INFODIR}/dir by hand."
fi
