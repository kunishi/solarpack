INFODIR=$CLIENT_BASEDIR/info
INSTALL_INFO=$CLIENT_BASEDIR/bin/install-info

if test -x ${INSTALL_INFO}; then
	${INSTALL_INFO} --delete --info-dir=${INFODIR} ${INFODIR}/m4.info
else
	/usr/bin/echo ">> Remove m4.info entry to ${INFODIR}/dir by hand."
fi
