if test ! -f $CLIENT_BASEDIR/etc/ssh_host_key; then
	$CLIENT_BASEDIR/bin/ssh-keygen -b 1024 -f $CLIENT_BASEDIR/etc/ssh_host_key -N ""
fi
if test ! -f $CLIENT_BASEDIR/etc/ssh_host_dsa_key; then
	$CLIENT_BASEDIR/bin/ssh-keygen -d -f $CLIENT_BASEDIR/etc/ssh_host_dsa_key -N ""
fi
