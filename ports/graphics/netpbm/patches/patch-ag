--- ppm/Imakefile~	Sun Aug 13 14:25:18 1995
+++ ppm/Imakefile	Fri Jun  2 13:44:19 2000
@@ -17,9 +17,9 @@
 #define DepLibPpm LibPpm
 #include <../Pbmplus.tmpl>
 
-   CURRENTLIBS = $(LIBPPM) $(LIBPGM) $(LIBPBM)
-CURRENTDEPLIBS = $(DEPLIBPPM) $(DEPLIBPGM) $(DEPLIBPBM)
-     INCLUDES  = -I.. -I$(PBMDIR) -I$(PGMDIR)
+   CURRENTLIBS = $(LIBPPM) $(LIBPGM) $(LIBPBM) $(LIBSHHOPT)
+CURRENTDEPLIBS = $(DEPLIBPPM) $(DEPLIBPGM) $(DEPLIBPBM) $(DEPLIBSHHOPT)
+     INCLUDES  = -I.. -I$(PBMDIR) -I$(PGMDIR) -I$(SHHOPTDIR)
        DEFINES = -DRGB_DB=\"DefaultRGBDatabase\"
          MERGE = ppmmerge
 
@@ -194,5 +194,5 @@
 #endif
 
 #if InstallIncludes
-InstallMultipleFlags(ppm.h,$(PBMPLUSDIR)$(PBMPLUSINCDIR),$(INSTINCFLAGS))
+InstallMultipleFlags(ppm.h ppmcmap.h ppmdraw.h,$(PBMPLUSDIR)$(PBMPLUSINCDIR),$(INSTINCFLAGS))
 #endif
